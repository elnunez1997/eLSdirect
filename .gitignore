# Excel workbooks — keep off version control (may contain PII / sensitive data)
*.xlsx
*.xlsm
*.xls

# Exception: repay letter templates are intentionally tracked
!repayletters/*.xlsx

# OS noise
Thumbs.db
.DS_Store
desktop.ini

# Local build output
dist/
node_modules/
